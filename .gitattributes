# Normalize text files to LF in the repo
* text=auto eol=lf

# Windows-only scripts (keep CRLF if you ever add these)
*.bat text eol=crlf
*.cmd text eol=crlf

# Binary files (never normalize)
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.pdf  binary